'use strict';

jest.dontMock('mongoose');
jest.dontMock('../message_manager');
jest.dontMock('../../models/message');
jest.dontMock('../../models/conversation');
jest.dontMock('../../models/user');
var User = require('../../models/user');
var MessageManager = require('../message_manager');

describe('Message Manager class', function () {

  beforeEach(function() {
  });

  it('look up username', function () {

  });

});